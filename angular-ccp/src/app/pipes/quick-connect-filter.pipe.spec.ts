import { QuickConnectFilterPipe } from './quick-connect-filter.pipe';

describe('QuickConnectFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new QuickConnectFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
